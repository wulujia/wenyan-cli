# 公众号风景油画配图规则

2026-09-24

- 发布流程增加配图搜索规则：封面和正文装饰性配图优先寻找优美的风景油画，查看实图，核对来源与许可，下载后写入文章并署名。
- 保留用户指定图片和说明事实的截图、图表。内置的 10 幅油画仅在缺少封面时作为备用来源；CLI 不搜索或选择正文配图。
- 同步发布文档，纠正“缺省封面直接使用正文首图”的旧说明，并注明须使用本 fork 构建。
- 修复 `--no-auto-cover`：Commander 输出 `autoCover: false`，原代码读取了不存在的 `noAutoCover`。本地及远程发布现均读取 `autoCover`。
- 新增发布命令回归测试，覆盖本地、远程两条路径的默认封面和关闭自动补封面的行为，发布调用使用替身，不上传微信草稿。

验证：回归测试在修复前失败、修复后通过。完整测试 50 项通过（使用临时配置目录并允许本地测试服务器监听端口）；类型检查、构建和 Skill 格式检查通过。Lint 无错误，保留 2 项原有未使用变量警告。未执行真实图片搜索或微信发布。
